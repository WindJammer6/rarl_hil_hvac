# Install PyTorch separately first so you can choose CPU or CUDA (using the command "conda install pytorch").
# Then run: pip install -r requirements.txt

tqdm==4.66.4
numpy==2.0.0
tensordict==0.4.0
torchrl==0.4.0
matplotlib==3.9.0
loading==0.0.1
pandas==2.2.2
gym==0.26.2
pythermalcomfort==2.10.0